CREATE TABLE Tasks (
  id_task  int(10) NOT NULL AUTO_INCREMENT, 
  content  text NOT NULL, 
  `create` date NOT NULL DEFAULT CURRENT_TIMESTAMP, 
  id_user  int(10) NOT NULL, 
  PRIMARY KEY (id_task));
CREATE TABLE Users (
  id_user    int(10) NOT NULL AUTO_INCREMENT, 
  name       varchar(50) NOT NULL, 
  email      varchar(50) NOT NULL, 
  password   varchar(200) NOT NULL, 
  token      varchar(255) NOT NULL, 
  permission int(10) NOT NULL, 
  online     date NOT NULL DEFAULT CURRENT_TIMESTAMP, 
  PRIMARY KEY (id_user), 
  UNIQUE INDEX (email));
ALTER TABLE Tasks ADD CONSTRAINT FK_Users_Tasks FOREIGN KEY (id_user) REFERENCES Users (id_user) ON UPDATE Cascade ON DELETE Cascade;
